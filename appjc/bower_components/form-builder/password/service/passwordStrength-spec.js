describe('passwordStrength', function() {

  beforeEach(module('formBuilder.password'));

  it('should ...', inject(function(passwordStrength) {

	//expect(passwordStrength.doSomething()).toEqual('something');

  }));

});